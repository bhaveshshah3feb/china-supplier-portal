name: Analyze & Auto-Fix Failures

on:
  workflow_run:
    workflows: ["Process Videos"]
    types: [completed]
  schedule:
    # Fallback: also runs at 4:30 AM UTC (10:00 AM IST) daily
    - cron: '30 4 * * *'
  workflow_dispatch:

permissions:
  contents: write   # required to commit auto-fixes to process-videos.mjs

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout master branch
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: master          # always checkout master, never detached HEAD
          fetch-depth: 0       # full history needed for rebasing

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Analyze failures and auto-fix
        env:
          SUPABASE_URL:         ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          CLAUDE_API_KEY:       ${{ secrets.CLAUDE_API_KEY }}
          GMAIL_USER:           ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD:   ${{ secrets.GMAIL_APP_PASSWORD }}
        run: node scripts/analyze-failures.mjs
